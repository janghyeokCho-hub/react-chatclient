const Config = {
  ServerURL: {
    HOST: `${window.covi.baseURL}`,
    CHAT: `${window.covi.baseURL}/server`,
    MANAGE: `${window.covi.baseURL}/restful`,
    EVENT: `${window.covi.baseURL}/`,
  },
};

export default Config;
